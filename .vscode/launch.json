{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Aggregate prices",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                "--bucket-name", "test-binance-prices",
                "--instrument-code", "ETHUSDT",
                "--count-years", "6"]
        }
    ]
}
